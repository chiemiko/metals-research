NOTES

docker build -t chiemiko/dockpractice .
docker build --tag test .

docker run -it ubuntu bash # to interact
docker run -p 80:8080 test

docker run python:3.6 cat /etc/*release*

# to run and publish exposed port 8080 (from Dockerfile) 
# to port 8383 and with file named webapp-color:lite
docker run -p 8383:8080 webapp-color:lite

# docker run -p means publish, 
# --name names the container, 
# -d is for backround detached mode 
# -e for overwriting parameters like APP_COLOR
# APP_COLOR adds a specific parameter
docker run -p 38282:8080 --name blue-app -e APP_COLOR=blue -d kodekloud/simple-webapp

docker rmi image_name # used for removing images
docker rm cntainer_name # used for removing containers 

docker images
docker ps

# Accessing linux through docker
# https://medium.com/@hudsonmendes/docker-have-a-ubuntu-development-machine-within-seconds-from-windows-or-mac-fd2f30a338e4

#! install vim
apt-get update
apt-get -y install vim
apt-get -y install python
apt-get -y install curl
apt-get -y install cron

export VISUAL=vim
crontab -e

# crontab Dockerfile: https://blog.softwaremill.com/editing-files-in-a-docker-container-f36d76b9613c
FROM openjdk:11
RUN ["apt-get", "update"]
RUN ["apt-get", "-y", "install", "vim"]